#include <iostream>

int main() {
    int file_size; // Snake Case (lower case letters, separate with underscore)
    int FileSize; // Pascal Case (Captitalize first letters)
    int fileSize; // Camel Case (captialize second letter)
    int iFileSize; // Hungarain Notation (starts with i, then Pascal Case)
}